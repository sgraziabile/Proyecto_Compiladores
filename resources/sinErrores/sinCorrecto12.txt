///[SinErrores]
//Test de visibilidades. Prueba con métodos, constructor y atributos.
    class Persona {
        private static int totalPersonas = 0;
        public String nombre;
        private int edad;
        private String nacionalidad;
        static int a;

        // Constructor
        public Persona(String nombre, int edad) {
            this.nombre = nombre;
            this.edad = edad;
            totalPersonas+=1;
        }

        // Método estático
        public static int obtenerTotalPersonas() {
            return totalPersonas;
        }

        // Método público
        public void mostrarInformacion() {
            // Lógica para mostrar la información de la persona
            var info = "Nombre: " + nombre + ", Edad: " + edad;
        }


        // Método privado
        private int calcularDecadas() {
            return edad / 10;
        }

        // Método sin modificador de visibilidad (paquete)
        void envejecer(int anios) {
            edad += anios;
        }
    }

    class ProgramaPrincipal {
        public static void main(String args) {
            var persona1 = new Persona("Juan", 30);
            var persona2 = new Persona("Ana", 25);

            persona1.mostrarInformacion();
            persona1.envejecer(5);

            var total = Persona.obtenerTotalPersonas();
        }
    }